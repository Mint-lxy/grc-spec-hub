# 横切规范：可观测性

> 状态：`[待确认]`。所有服务共同遵守。

## 日志
- 结构化 JSON；必带 `trace_id`、`service`、`spec`（关联 spec 编号，如有）。
- 禁止记录 PII 明文与凭据。

## 指标
- 每服务暴露：请求量/延迟/错误率（RED）；关键业务指标另行声明。

## 追踪
- 分布式追踪跨契约调用传播上下文：`[待确认: W3C traceparent]`。

## 关联
错误码与幂等约定见同目录 `error-codes.md`、`idempotency.md`。

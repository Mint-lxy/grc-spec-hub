# 横切规范：错误码与幂等

> 状态：`[待确认]`。所有服务共同遵守。

## 错误响应格式（REST）
统一错误体（建议 RFC 9457 Problem Details 或等价）：
```json
{ "type": "...", "title": "...", "status": 400, "detail": "...", "traceId": "..." }
```
- 错误码命名空间按服务前缀：`ORDER-xxxx`、`BILLING-xxxx`。

## 幂等约定
- 写操作应支持幂等：`[待确认: 幂等键放 body 还是 header]`。
- 事件消费者必须幂等（至少一次投递假设）。

## 重试与超时
- 跨服务调用默认超时与重试策略：`[待确认]`。

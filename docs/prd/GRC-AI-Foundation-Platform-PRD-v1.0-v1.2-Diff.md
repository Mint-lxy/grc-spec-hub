# PRD v1.0 → v1.2 差异对比与决策理由

| 项目 | 内容 |
| --- | --- |
| 对比文件 | `GRC-AI-Foundation-Platform-PRD-v1.0.md`（2026-08-13，specs 拆解基线）→ `GRC-AI-Foundation-Platform-PRD-v1.2.md`（2026-08-21，当前基线） |
| 更新日期 | 2026-08-22（第三版：补入 PRD §13.10 全部收口与契约兼容微调；不做破坏性契约字段删除或重命名） |
| 生成方法 | 两份全文逐节比对（约 1200 行/份），不以版本说明为限；版本说明漏记项已兜底补入 |
| 关联文档 | `CONTEXT.md` 与 `docs/adr/`（BA 工作区）、`../../memory/now/watchlist.md`（待确认清单） |
| 结论速览 | 章节结构完全一致（无增删重排）；变更主线 10 条；用户故事 154 → 157 条；通知 13 → 12 类；待确认事项 16 → 0 项在办（全部确认或关闭）；参数基线 24 → 28 行且全部审定；四张状态机节点与边全部不变 |

## 一、变更主线与理由

以下 10 条主线按名称引用，后文差异表的「理由」列沿用同一组名称。

### 1. Chat 能力收窄与原生工具归位

**内容**：平台 Chat agent-runtime 能力从三项收窄为两项（模型对话、知识库检索）；平台原生工具（Confluence、SharePoint/OneDrive、OSS 三个 MCP）不再是 P0 对话能力，仅作知识库外部导入通道；取消「对话场景平台内置统一凭据」（注册表平台内部条目仅余 Nexus 令牌）；保留「P0 不开放经 PAT 调用原生工具」；对话工作台新增资产 Agent 选择器入口；既有接口中的工具调用承载兼容保留，不构成 P0 能力承诺，并为 P1 Chat 调用已订阅 MCP 资产工具能力预留。

**理由**：对话场景使用平台内置统一凭据等于以平台身份读取来源系统，存在内容权限放大风险；仅作导入通道后，权限防线落在操作人本人在来源系统的可读范围。与用户旅程 v1.1/v1.2 对齐；ADR-0015 同步修订。

### 2. 凭据按使用场景区分

**内容**：取消「服务间鉴权／按人鉴权」的资产级声明概念——创建资产不再声明鉴权主体类型；一切资产均须配置资产级服务凭据（质量门控对应项去掉限定）；平台内资产调用统一使用资产级服务凭据并传递当前用户身份上下文；平台外资产调用使用平台为订阅者签发、绑定用户与资产的个人出站凭据，未生成/失效/失败即调用失败且不回落任何共享身份；MCP 创建向导鉴权配置由「仅静态凭据」扩为「静态＋动态」（能力扩展）。

**理由**：Owner 无需理解并声明鉴权主体类型，降低上架门槛、消除声明与实际不一致的空间；平台内调用传递用户身份上下文，使资产内部可做用户级数据权限；平台外调用身份真实、可撤销、不回落。ADR-0014 同步修订。

### 3. 取消人工推荐位

**内容**：频道子页签三个→两个（删「推荐」）；删除管理后台推荐位管理、下架退出推荐位规则、推荐位参数行与相关验收点；两条相关用户故事删除；入「不承接」清单（非降级为二期）。

**理由**：人工运营位的治理责任与运营成本在首版未定义；热门榜（真实采用度驱动）＋全部列表＋搜索已足够支撑资产发现。

### 4. 知识治理收紧与分享授权

**内容**：知识库创建角色与维护角色分离（建库能力≠维护权限）；我的目录新增平台内分享授权体系（可查看/可编辑两级、「分享给我的」子分区、可撤销、账号失效即失效、不写入 Alice）；随之扩展导入与删除的执行者范围、开放接口按分享授权分级、挂载范围含被分享库；新增四个首版功能点：知识库重命名、知识库详情指标与就绪度、文档级参数批量修改、回草稿引导文案；建库向导三步→两步（删纯展示确认步）。

**理由**：权责分离，维护授权归口审批；小范围协作不必走企业授权流程，是 Alice 唯一权威原则的显式例外；重命名与就绪度补齐建库后的管理闭环；纯展示步骤无决策价值。

### 5. 通知事件收敛

**内容**：删除独立的「资产重新上架」通知；「资产发布新版本」吸收重新上架场景（从「已下架」完成的发布，通知下架时刻的存量订阅者与待审批申请人）。十三类收敛为十二类。

**理由**：重新上架本质就是一次版本发布（版本号递增），单列事件造成口径分裂与重复通知。

### 6. 取消会话级快照

**内容**：知识库检索不设会话级快照，收敛事件立即生效且按事件性质区分语义：停用/软删除＝向量检索层不可用（无会话侧主动停用要求）、回「草稿」＝库内即无可检索内容、权限回收＝检索时权限校验拒绝；进行中的会话与新会话一视同仁；历史会话 90 天留存与只读回看不受影响。

**理由**：原快照口径下「停用仅对新会话生效」，与「停用即收敛使用面」的止损时效期望存在敞口（进行中会话最长可持续 90 天）。立即生效消除该敞口。实现侧注意：服务拆分决策中「会话创建时缓存知识库清单」的结论需随之复核（watchlist 架构复核项）。ADR-0013 加补充。

### 7. 用户故事补遗

**内容**：为差异收敛新增的五个首版功能点补齐用户故事——创作者中心统计卡、知识库就绪度、知识库重命名、我的目录分享授权（创建者侧与被分享者侧两条）；批量修改文档级参数已有覆盖不新增。总数 152 → 157 条。

**理由**：差异收敛引入的功能点原先没有对应用户故事，存在「功能项—用户故事—spec」三层可追溯链的缺口。

### 8. 待确认事项澄清与参数基线审定

**内容**：五项确认关闭（Key Vault 集成、资产级服务凭据可行性、在途申请条款版本口径、外部依赖契约——含「模型类资源全部经 Nexus 供给」、止损时效经第 6 条设计变更关闭）；参数基线 28 行逐项 interview 审定完毕，成为工单基线。开工前必裁项全部关闭。

**理由**：2026-08-21 用户逐项裁定。参数基线的 6 处调整与 4 个落实值见专项汇总第 6 节。

### 9. 建库角色新模型

**内容**：公共目录建库时，创建者须同时指定知识库 Owner 与知识库 Deputy（可为创建者本人）；平台据此自动调用 Alice 创建三个维护角色，并将知识库 Owner 角色授予指定的 Owner 与 Deputy（权限等同，负责人冗余与资产侧同构）；三个维护角色的后续授予与回收改由知识库 Owner/Deputy 单级审批（不再由上级目录控制角色审批）；两者全部失效时由上级目录控制角色补员；创建者未自任时自动获得知识库 Consumer（可检索可读取，不可维护）。

**理由**：这是第三版模型，取代前两版的各自缺陷——初版「自动授予创建者」权责不分；差异收敛版「不自动授予、维护角色需单独申请审批」产生建库后无人可维护的空窗期。新模型兼有权责分离与无空窗。ADR-0013 加补充。

### 10. 业务规则补遗（spec 细化评审回写）

**内容**：订阅侧新增——同一申请人对同一资产至多一条在途申请（订阅入口禁用提示）、禁止自审（各级审批人计算排除申请人本人）、Owner 与 Deputy 对自有资产免订阅可用、整单驳回后其余级待办清除且驳回理由必填（建议补充项选填）、同级并发审批以最先落库动作为准、在途申请对申请人只读、审批进展展示各级状态与审批人及处理时间、创作者中心统计卡可点击钻取、热门榜统计口径（有效订阅＋实际调用不回滚）、收藏设上限；知识侧新增——停用仅收敛消费面（维护侧不受限）、文档级参数按最早受影响阶段重跑、知识库名称同目录内唯一、更新内容接口同名冲突由调用方参数选择并自动触发构建、检索开放接口 top-k 可由调用方自定义（设上限）、向量数取向量库实际写入条数、分享授权约束集（不发通知、全留痕、撤销免确认、人数上限、仅逐人分享、可查看者详情页只读）、建库 Alice 角色创建失败整体回滚、文档删除不可恢复、切片与参数编辑的版本校验乐观锁、跨库构建并行加平台级并发上限、切片编辑支持删除单个切片、Overview 内容摘要是静态元信息汇总（AI 生成库级摘要入二期）。

**理由**：spec 002/005 经逐项评审（grill 与 clarify）确认的决策回写，保持 PRD 为唯一业务基线；各项具体理由以行内 rationale 记录于 spec 002/005 正文。

## 二、按章节详细差异表

> 「理由」列引用第一节的主线名称；标「—」的为措辞对齐或结构性微调。

| PRD 位置 | 变更类型 | 变更摘要（v1.0 → v1.2） | 理由 |
| --- | --- | --- | --- |
| 头部元数据 | 修改 | 版本 v1.0（设计基线版）→ v1.2（差异收敛版）；词汇依据 CONTEXT.md 40 → 47 条 | — |
| 版本说明 | 新增 | 新增差异收敛条目与四条补遗条目（用户故事、待确认澄清、业务规则、建库角色模型） | — |
| 名词约定·专有名词 | 修改 | 「平台原生工具」词条改为仅导入通道、导入用操作人个人出站凭据 | Chat 能力收窄与原生工具归位 |
| 名词约定·易混淆词 | 修改 | 资产级服务凭据→「用于平台内资产调用」；个人出站凭据→「用于平台外资产调用」 | 凭据按使用场景区分 |
| 解决方案·对话工作台 | 修改 | Chat 能力两项化；新增资产 Agent 选择器入口 | Chat 能力收窄与原生工具归位 |
| 解决方案·安全底座 | 修改 | 出站个人凭据目标改为「平台外资产调用」 | 凭据按使用场景区分 |
| 解决方案·知识中枢 | 修改 | Alice 权威限定为公共目录；新增我的目录分享授权例外 | 知识治理收紧与分享授权 |
| 解决方案·管理后台 | 删除+修改 | 删推荐位管理；注册表平台内部条目仅余 Nexus 令牌 | 取消人工推荐位；Chat 能力收窄与原生工具归位 |
| 用户角色·普通用户 | 修改 | 凭据描述按平台内/平台外场景改写 | 凭据按使用场景区分 |
| 用户角色·订阅审批人 | 修改 | 分区名「我审批的资产」→「待我审批」 | — |
| 用户角色·平台管理员 | 删除 | 删除「管理推荐位」能力 | 取消人工推荐位 |
| 用户角色·知识侧角色 | 修改+新增 | 我的目录「默认不存在 Alice 角色概念」；新增分享授权；知识库 Owner 行补「建库时指定、另设知识库 Deputy」 | 知识治理收紧与分享授权；建库角色新模型 |
| 用户角色·权限模型 | 修改 | Alice 唯一权威限定为账号、角色与公共目录知识授权；分享授权为例外 | 知识治理收紧与分享授权 |
| 首版页面清单 | 修改+新增+删除 | 创作者中心四统计卡+分区改名；删推荐位管理；新增「准入测评模板维护」页面（spec 归属待确认） | 取消人工推荐位；测评模板页为既有决议的页面落位补齐 |
| 用户旅程·资产发现 | 删除 | 发现入口删「推荐位」 | 取消人工推荐位 |
| 用户旅程·订阅申请 | 修改 | 单页底部新增当前版本发布说明；条款默认收起；审批人链概览可展开 | 订阅决策信息一屏看全 |
| 用户旅程·对话与使用 | 修改 | 新增选择器入口；凭据语义按场景改写；Chat 删原生工具调用 | Chat 能力收窄与原生工具归位；凭据按使用场景区分 |
| 用户旅程·订阅者通知 | 修改+删除 | 通知事件删「资产重新上架」 | 通知事件收敛 |
| 用户旅程·创建向导 | 修改 | 鉴权配置步删「声明鉴权主体类型」；门控项对一切资产生效；热改清单补凭据密钥值；轻发布受众补待审批申请人 | 凭据按使用场景区分 |
| 用户旅程·低代码（二期） | 修改 | 新增「从 Dify 工作区已有 Agent 列表勾选接入」入口 | 二期体验改进 |
| 用户旅程·建库 | 修改+新增 | 三步→两步；建库时指定知识库 Owner 与 Deputy；分享授权 | 知识治理收紧与分享授权；建库角色新模型 |
| 用户旅程·知识库管理 | 新增 | 批量修改文档级参数；开放接口按分享授权分级；知识库重命名 | 知识治理收紧与分享授权 |
| 用户旅程·治理中心 | 修改+删除 | 注册表内部条目仅余 Nexus 令牌；推荐位管理改不承接 | Chat 能力收窄与原生工具归位；取消人工推荐位 |
| 用户旅程·凭据 | 修改 | PAT 链路与出站凭据按场景改写；导入通道含可编辑分享授权用户 | 凭据按使用场景区分；知识治理收紧与分享授权 |
| 功能清单·知识库挂载 | 修改 | 挂载范围含被分享知识库 | 知识治理收紧与分享授权 |
| 功能清单·Chat 与透传 | 修改 | 选择器入口；Chat 能力两项化；原生工具功能项改名「接入」仅导入通道；不可用文案四类→三类 | Chat 能力收窄与原生工具归位；凭据按使用场景区分 |
| 功能清单·发现与榜单 | 删除 | 子页签三→两，删「推荐」及下架退出规则 | 取消人工推荐位 |
| 功能清单·资产卡片与详情 | 删除+修改 | 卡片删「按人鉴权」标注；详情删鉴权主体类型字段；MCP 凭据状态页签按场景改写 | 凭据按使用场景区分 |
| 功能清单·订阅申请与中心 | 修改+新增 | 申请页补发布说明；订阅者中心措辞；创作者中心四统计卡；分区改名 | 知识治理收紧与分享授权（统计卡后由用户故事补遗补齐） |
| 功能清单·资产创建 | 修改 | 向导删鉴权主体类型；MCP 向导扩为静态＋动态凭据；门控去限定 | 凭据按使用场景区分 |
| 功能清单·知识管理 | 修改+新增 | 建库角色模型（指定 Owner+Deputy）；重命名；详情指标与就绪度；批量参数；执行者按分享授权分级 | 知识治理收紧与分享授权；建库角色新模型 |
| 功能清单·凭据 | 修改 | 凭据管理面拆分（实例类随注册表、模型类随模型设置）；平台外调用凭据语义与动作集；开放接口清单按场景改写 | 凭据按使用场景区分 |
| 功能清单·管理后台 | 修改+删除 | 原生工具仅导入通道；注册表内部条目仅余 Nexus 令牌；推荐位不承接 | Chat 能力收窄与原生工具归位；取消人工推荐位 |
| 功能清单·通知 | 删除+修改 | 十三类→十二类；发布通知吸收重新上架 | 通知事件收敛 |
| 用户故事 | 修改 | 154 → 152 → 157 条（明细见专项汇总） | 取消人工推荐位；用户故事补遗 |
| 资产生命周期规则 | 修改+删除 | 重上架通知口径改写；删「下架与推荐位」整条规则 | 通知事件收敛；取消人工推荐位 |
| 版本模型与配置两分 | 修改 | 版本类字段枚举删「鉴权主体类型」；轻发布受众补待审批申请人 | 凭据按使用场景区分 |
| 订阅状态机规则 | 修改+新增 | 重新上架不单独通知；主体侧失效含分享授权；后补在途申请唯一/禁止自审/Owner 免订阅/驳回清理/在途只读/并发裁决 | 通知事件收敛；知识治理收紧与分享授权；业务规则补遗 |
| 凭据规则 | 修改 | 凭据管理面与解析规则按场景口径全面改写 | 凭据按使用场景区分 |
| 知识生命周期规则 | 修改+新增 | 就绪度规则；回草稿提示文案；执行主体扩展；后补停用仅收敛消费面/重跑起点/指标口径/构建并发；会话级快照规则删除 | 知识治理收紧与分享授权；取消会话级快照；业务规则补遗 |
| 知识治理规则 | 修改+新增 | 建库角色新模型（指定 Owner+Deputy、库级审批归口）；我的目录分享授权整段；可见范围例外注记；后补分享约束集 | 建库角色新模型；知识治理收紧与分享授权；业务规则补遗 |
| 知识授权审批 | 修改 | 单级审批按目录级/库级拆分归口（库级归知识库 Owner/Deputy） | 建库角色新模型 |
| 参数基线 | 删除+新增+审定 | 删推荐位行；净增 4 行（分享上限、名称约束、收藏上限、构建并发）并拆目录层级为公共/我的两行；全部 28 行逐项审定 | 取消人工推荐位；业务规则补遗；待确认事项澄清与参数基线审定 |
| 模块划分与决策索引 | 修改 | 对话引擎删原生工具调用；密钥库与管理后台口径同步；决策索引含「不设会话级快照」 | 各主线综合 |
| 验收点 | 修改+删除 | 凭据/轻发布/对话边界/通知条按新口径改写；删推荐位验收点 | 各主线综合 |
| 首版交付范围 | 修改 | 各模块交付描述同步 | 各主线综合 |
| 部署初始化清单 | 修改 | 原生工具行与注册表初始条目行改写 | Chat 能力收窄与原生工具归位 |
| 不承接清单 | 修改+新增 | 新增「Marketplace 人工推荐位」；凭据兜底行重写 | 取消人工推荐位；凭据按使用场景区分 |
| 历史裁决附录 | 修改 | 上一轮裁决摘要按 v1.2 口径同步改写（历史附录，spec 无需跟随） | — |
| 待确认清单 | 修改 | §13.10 共 16 项全部确认或关闭，并在表后记录各项结论 | 待确认事项澄清、参数基线审定与 2026-08-22 BA 裁定 |

## 三、专项汇总

### 1. 用户故事（154 → 157 条）

- **删除 2 条（编号不重排）**：推荐位浏览、推荐位管理——随「取消人工推荐位」。
- **新增 5 条（字母后缀插入，既有编号不重排）**：创作者中心统计卡、知识库就绪度、知识库重命名、分享授权（创建者侧与被分享者侧各一条）。
- **实质修改 12 条（编号不变）**：资产卡片标注、凭据场景化两条、通知口径、鉴权配置简化两条、轻发布受众、批量修改参数、平台外凭据签发、注册表内部条目、重上架通知、分区改名。

### 2. 通知类型（13 → 12 类）

删除独立「资产重新上架」通知；「资产发布新版本」吸收重新上架场景。其余 11 类不变。

### 3. 待确认事项（16 → 0 项在办）

- **2026-08-21 关闭 6 项**：Key Vault 集成、资产级服务凭据可行性、在途申请条款口径、外部依赖契约（含「模型类资源全部经 Nexus 供给」）经确认关闭；止损时效经「取消会话级快照」设计变更关闭；参数基线逐项审定完毕关闭。
- **2026-08-22 BA 裁定关闭剩余 10 项**：留存周期隐私、知识目录权限细化、事件响应边界、二期自助测评范围、临时文件检测时机（二期）、重复文档判重（二期）、内容源权限放大远期口径、定时同步凭据主体（二期）、申请撤回（产品决策）、审批停滞兜底（产品决策）。
- 编号固定不重排；结论记录于待确认清单表后「已确认与已关闭事项」。

### 4. 首版/二期范围

- 无批次迁移；推荐位为直接取消（不承接）。
- 首版新增：资产 Agent 选择器入口、知识库重命名/就绪度/批量参数/分享授权、创作者中心统计卡、回草稿提示，以及业务规则补遗全部规则。
- 首版能力扩展：MCP 向导支持动态凭据；检索开放接口 top-k 可自定义（设上限）。
- 二期修改未升批：低代码 Agent 第二入口、AI 生成库级摘要（新列入）。

### 5. 状态机

资产六态、订阅状态机、知识库三态、文件五态的节点与边全部不变；变化在执行主体（分享授权用户获得 Contributor 语义动作）、提示文案与检索时效语义（取消会话级快照）。

### 6. 参数基线（24 → 28 行，全部审定）

- 删 1 行：推荐位每频道置顶数上限。
- 拆 1 行：目录层级拆为公共目录（5 层/20 个一级）与我的目录（3 层）两行。
- 净增 4 行并审定：我的目录单库分享人数上限 20 人；知识库名称 1–100 字符且禁控制字符与路径分隔符；资产收藏上限 50 个；平台级并发构建流水线 5 条（超出排队）。
- 审定调整 6 处：Health Check 探测周期 5 → 10 分钟；检索去重由「按来源文档」改为「按切片」；会话 500 个上限补充删除按钮说明；PAT 有效期由「最大 365 天」改为「允许永不过期（默认 90 天）」；审批级数上限 5 → 3 级。
- 其余数值全部不变。审定后即为工单基线（待确认清单相应项关闭）。

## 四、对 specs 的影响映射

| spec | 涉及主线 | 同步状态（2026-08-21） |
| --- | --- | --- |
| 000-platform | — | 无需变更 |
| 001-conversation | Chat 能力收窄与原生工具归位；凭据按使用场景区分；知识治理收紧与分享授权；取消会话级快照 | 已同步（验收条目 30→26 重编号） |
| 002-marketplace | 凭据按使用场景区分；取消人工推荐位；业务规则补遗 | 已同步 + 重写为 spec-kit 格式 + 三轮评审细化 |
| 003-asset-creation | 凭据按使用场景区分 | 已同步（表单保存时强校验联动 002） |
| 004-publish | 取消人工推荐位；通知事件收敛 | 已同步（验收条目 29→28 重编号） |
| 005-knowledge | 知识治理收紧与分享授权；取消会话级快照；业务规则补遗；建库角色新模型 | 已同步 + 两轮评审细化（FR 54 → 61 条+补充条） |
| 006-guardrail | — | 仅头部版本标注 |
| 007-credential | Chat 能力收窄与原生工具归位；凭据按使用场景区分；知识治理收紧与分享授权；待确认事项澄清 | 已同步；未决问题清零 |
| 008-admin | Chat 能力收窄与原生工具归位；取消人工推荐位；待确认事项澄清 | 已同步（验收条目 20→19 重编号）；测评模板页归属待确认 |
| 009-notification | 通知事件收敛 | 已同步（验收条目 18→17 重编号） |

# 2026-09-23

## 把工作区全部改动同步到 GitHub（official-version）

- 用户要求：`https://github.com/Mint-lxy/grc-spec-hub/tree/official-version`，本文件夹所有更改以其账号同步到该仓库，之前的不管了。
- 结果：已全量同步完成，未使用 force push（远程分支原本就在 05bf1b1，属快进）。

### 远程与分支

- 新增 git 远程 `github` → `https://github.com/Mint-lxy/grc-spec-hub.git`；原有 `origin`（Mercedes GHE `china/grc-spec-hub`）**未改动，保持不变**。
- GitHub 仓库为公开仓库，`git ls-remote` 免认证；push 由 Git Credential Manager 提供凭据，免交互成功。本机无 `gh` CLI。
- 推送对象：本地分支 `official-version`（推送前与 `github/official-version` 同为 `05bf1b1`）。
- 提交：`0fcd7b4 docs: sync local workspace state to official-version`（9 files changed, +1544 / -7）。
- 已设 upstream：`official-version` 跟踪 `github/official-version`；工作区 HEAD 现停在 `official-version`（不再是 `docs/knowledge-enable-disable-spec`，该分支也在 `0fcd7b4`）。
- GitHub 侧其它分支未动：`main` = 4f9ff18、`sync/2026-09-02-knowledge-meeting` = affb328（本地无对应分支实体，仅 remote-tracking）。

### 本次提交内容

- `specs/001-conversation/spec.md`：可挂载知识库范围对齐 spec 005 FR-049（三类公共目录口径）；Assumptions 悬空引用「ADR-0015」更正为 ADR-001；新增 Clarifications Session 2026-09-21。
- `specs/005-knowledge/spec.md`：US11 场景 6 与 FR-048 的「资产 Agent 透传会话」统一为「资产 Agent 实验台（playground）」，语义不变；新增 Clarifications Session 2026-09-21；不增删 FR。
- 新增工作副本 `specs/001-conversation/spec 3.md`、`specs/001-conversation/spec-playground.md`。
- 新增本地工作记忆 `.codebuddy/memory/2026-09-17|20|21|22.md` 与 `MEMORY.md`（本次一并纳入版本控制，`git add -A`）。

### 坑位

- PowerShell 下 `git push` 的 stderr 会被当作 `NativeCommandError` 标红报错，属噪声；以最后一行 `05bf1b1..0fcd7b4 official-version -> official-version` 判断成功。
- 含空格的文件名（`spec 3.md`）用 `git add -A` 可正常处理，无需手工引号。

## 追加：清空历史，改为单条「导入」提交

- 用户反馈：GitHub 上旁边一堆提交记录「不要了，那是之前的仓库」；并要求提交作者用其账号 `mintli`。
- 做法：`git tag archive/pre-orphan-20260923 3369a6f`（本地留旧历史备份，未推送）→ `git checkout --orphan _orphan_tmp` → `git add -A` → 单条 root commit `1789c2f chore: import grc-spec-hub official-version (2026-09-23)`（344 files, +92041）→ 删除原 `official-version` 后改名复用 → `git push -f -u github official-version`。
- 校验：`git diff --stat 3369a6f HEAD` 为空，文件树与清理前完全一致，只是历史被压成一条。
- **提交作者身份**：用 `git -c user.name=mintli -c user.email=mintli@users.noreply.github.com` 逐次内联覆盖；**未改动任何持久 git config**（全局仍是 Zeng Ziyang <zizeng@deloittecn.com.cn>）。若要让后续提交默认走 mintli，需用户自行执行 `git config user.name mintli` / `git config user.email mintli@users.noreply.github.com`。
- **旧分支处理结论（2026-09-23 用户裁定）**：GitHub 上的 `main`（4f9ff18，默认分支）与 `sync/2026-09-02-knowledge-meeting`（affb328）**都不删**，保留现状；只要求 `official-version` 保持单条干净历史。后续不要主动清理这两个分支。
  （备注：`main` 是 GitHub 默认分支，不经网页先改 Default branch 就无法用 git 删除。）
- 推送含历史重写的分支用 `-f`；PowerShell 下 stderr 报红属噪声，认最后一行 `+ 3369a6f...1789c2f official-version -> official-version (forced update)`。

## 追加：本地只留 GitHub 一个远程

- 用户要求清掉 git 图里那些旧提交（截图里带 `origin/docs/kno...` 标签的一串）。这些是**公司 GHE 远程的 tracking ref**，不在 GitHub 上。
- 处理：
  - `git remote remove origin`（公司 GHE `mercedes-benz@mercedes-benz.ghe.com:china/grc-spec-hub.git`）——本地现在**只有 `github` 一个远程**。
  - 删除全部指向旧历史的本地分支共 9 个：`backup/003-rewrite-devin`、`clarify/001-conversation`、`docs/knowledge-enable-disable-spec`、`main`、`review/006-guardrail-final`、`review/008-admin-final`、`spec-sync-prd-v1.2`、`spec/005-priority-notation-34`、`watchlist/nav-ia-conflict-79`。
  - 删除兜底 tag `archive/pre-orphan-20260923`。
  - 本地 ref 只剩 `refs/heads/official-version` = 单条根提交（`git rev-list --parents -1` 无父提交 → 不携带任何旧历史）。作者与提交者均为 `mintli <mintli@users.noreply.github.com>`。
- **未删**：公司 GHE 服务端的旧分支内容（只删了本地远程指针）；GitHub 上的 `main`(4f9ff18) 与 `sync/2026-09-02-knowledge-meeting`(affb328)（用户裁定保留，这两个本身就是旧完整历史，在 GitHub 上公开可见）。
- **残留事实（重要）**：GitHub 上被强推覆盖的旧对象（原 `official-version` 那条含公司内部提交的线）会以不可达对象形式在服务器上停留一段时间；要彻底清除需删库重建。本地不可达对象也可由 reflog 找回一段时间。
- 教训：反复 `commit --amend` + `push -f` 会在远端累积悬空对象，发布/镜像场景要尽量一次成型。

# 2026-09-17

## grc-spec-hub 同步到 GitHub `official-version` 分支

- 用户要求：在 `https://github.com/Mint-lxy/grc-spec-hub` 新建分支「official version」，把当前工作区内容同步上去。
- 关键事实：git 分支名不允许包含空格（`git check-ref-format --branch "official version"` → `fatal: not a valid branch name`），实际落地分支名为 **`official-version`**（已向用户说明，可随时改名）。
- Remote 现状：
  - `origin` = `mercedes-benz@mercedes-benz.ghe.com:china/grc-spec-hub.git`（内网 GHE，**未改动**）。
  - GitHub 目标仓库通过 URL 直推，**未新增 remote 配置**（遵循不改 git config 的约束）。
- 执行步骤：
  1. 提交工作区未提交改动 `specs/005-knowledge/spec.md`（新增「已知测试问题（2026-09-13 用户确认）」表格）→ commit `05bf1b1`，位于分支 `docs/knowledge-enable-disable-spec`。
  2. 基于 `05bf1b1` 创建本地分支 `official-version`（用 `git branch`，**未切换 HEAD**，仍停在 `docs/knowledge-enable-disable-spec`）。
  3. `git push https://github.com/Mint-lxy/grc-spec-hub.git official-version` → 成功，`* [new branch]`。
- 校验结果：远端 `refs/heads/official-version` = `05bf1b1`，与本地一致；远端另有 `main` = `4f9ff18`、`sync/2026-09-02-knowledge-meeting` = `affb328`。
- 未做事项：未设置 upstream 跟踪；未 push 到 `main`；未触碰 `origin`。
- 后续注意：因未加 remote，再推送需重复使用完整 URL；是否 `git remote add github <url>` 待用户决定。

## Spec 001「对话工作台」功能清单梳理与导出

- 用户要求：读 spec 文档 → 列出 conversation 功能清单 → 导出成 md。
- 事实源：`specs/001-conversation/spec.md`（Status: Reviewed，最近修订 2026-09-15），辅以 `plan.md`；另有 `specs/001-conversation/tasks.md`、`checklists/requirements.md` 未逐一展开。
- Spec 001 结构（便于后续直接定位，无需重读全文）：
  - 8 大功能模块 / 38 条 FR：A Chat 对话对象定位（FR-001~003）；B 主界面与基础能力含多模态图像输入（FR-004~009、009a~009e）；C 知识库挂载与检索（FR-010~016）；D 资产 Agent 透传会话（FR-017~022）；E 流式输出与护栏（FR-023~027）；F 回答渲染（FR-027b/027c）；G 历史会话管理（FR-028~032）；H 我的资产抽屉与不可用提示（FR-033~036）。
  - 优先级：P1 = US1 基础对话与图像识别 / US2 知识库挂载检索 / US3 流式输出与护栏；P2 = US4 透传会话 / US5 历史会话 / US6 我的资产抽屉。
  - 关键数值基线：挂载上限 5 库、归并口径 5/10 按切片去重、会话上限 500、持久化与留存 90 天、审计 3 年、图像 ≤4 张 / 10 MiB 单张 / 20 MiB 总量 / 超时 120s。
  - 开放问题：滑动窗口参数与检测引擎选型（watchlist #1/#50）、Nexus Base64 图片 PoC 未做。
  - 涉及服务：`grc-agent-service`（新增，无状态）、`grc-mgmt-service`（会话/挂载/消息权威）、`grc-knowledge-engine`（检索）、`grc-api-gateway`（路由透传）。
- 交付物：`C:\Users\mintli\OneDrive - Deloitte (CN)\Documents\GRC-AI平台-Spec001-对话工作台-功能清单-2026-09-17.md`（约 20.6 KB，含 8 模块清单 + 优先级 + 非目标 + 开放问题 + 关键裁定 + 涉及服务/契约 + 附录 A 验收口径 SC-001~011 / B 原 AC 编号对照 / C 跨 spec 引用）。
- 约束遵守：**未改动工作区任何文件**（只读取 hub 事实源），交付物落在用户指定的 OneDrive Documents 目录。

## Spec 001 功能清单导出件 v2 增「一期」列

- 用户要求：在 OneDrive Documents 的功能清单 md 上**新增一列「一期」**，每条 FR 后标 ✓（属本期）或留空（非本期）。
- 事实：spec 001 现有 38 条 FR（FR-001~FR-036 含 FR-009a~009e、FR-027b/027c）全部属本期；非目标项（第 3 节）无 FR 编号、本就独立成节。
- 一期口径：**本期 = P0 = M1 + M2**（对齐 `specs/000-platform/spec.md` 里程碑表，M3 为 P1 二期交付）；spec 001 内部 US 优先级 P1/P2 是 feature 落地排序，**与期次无关**。
- 操作：① 8 个 FR 子表（A–H）表头各加 `| 一期 |` 列（共 8 处）；② 43 行 FR 数据行（含 FR-009a~e、FR-027b/c）各加 `| ✓ |` 结尾；③ 「0. 总览」补「本期口径」bullet；④ 「1. 功能清单」开头加列说明；⑤ 「4. 尚未闭合的开放问题」第 2 条更新为 spec/tasks 漂移说明（T120 已勾 vs spec 未回写）。
- **事故与恢复**：第一次用 PowerShell `Get-Content` 没指定编码，把 UTF-8 文件读成 Windows 默认编码乱码并写回，导致中文全部损坏。**修复方式**：用 `write_to_file` 整文件重建（UTF-8 无 BOM，21,511 字节，43 条 FR 全部 ✓）。**教训**：处理包含非 ASCII 内容的文件时，PowerShell 5.1 必须用 `Get-Content -Encoding UTF8` 和 `[System.IO.File]::WriteAllText($path, $content, (New-Object System.Text.UTF8Encoding $false))`，**不要用默认编码 `Get-Content` / `Set-Content`**。
- 文件：`C:\Users\mintli\OneDrive - Deloitte (CN)\Documents\GRC-AI平台-Spec001-对话工作台-功能清单-2026-09-17.md`

## Chat 功能测评提示词套件（Spec 001 对齐 + 市面对标）

- 用户要求：根据 spec 中 chat 部分，出一套测评 chat 设计功能的提示词；可参考市面上能关联知识库的 chat。
- 事实源：`specs/001-conversation/spec.md`（38 条 FR，2026-09-15 版）+ `specs/005-knowledge/spec.md`（引用/检索/停用无快照）+ `specs/006-guardrail/spec.md`（四类护栏：提示词注入、有害内容、意图偏离、违禁词，不含 PII）+ `specs/001-conversation/plan.md`。
- 交付物结构（三组提示词）：**A 组** 53 条功能探测提示词（P-01~P-53，10 个维度 D1~D10，每条含目标 FR / 可复制提示词 / 期望结果 / FAIL 特征）；**B 组** LLM-as-judge 评审提示词（system prompt + 单用例 + 批量汇总，JSON schema 含 verdict/matched/violated/missing_evidence/severity）；**C 组** 12 条市面对标同题提示词（X-01~X-12，对标 Glean/M365 Copilot/ChatGPT Enterprise/Notion AI/飞书智能伙伴/ima/Dify/RAGFlow 等）+ 14 项横评记录表。
- 另含：第 2 节测试夹具（KB-A/B/C/D/E/X1-5/STOP + IMG-1~10 + 4 类账号）、第 6 节评分卡（权重：D4 知识库挂载 18% + D5 引用渲染 14% 最高）、通过线（blocker=0 且 D1/D4/D5/D7 ≥90%，整体 ≥90%）、缺陷记录模板、回归子集 15 条。
- 合规处理：护栏用例不列举实际违禁词/有害内容样本，统一用占位标记 `【违禁词样本-按护栏模板词表填入】`，并限定受控环境 + 安全负责人批准。
- 文件：`C:\Users\mintli\OneDrive - Deloitte (CN)\Documents\GRC-AI平台-Chat功能测评提示词套件-2026-09-17.md`（36,323 字节，含 1 张 Mermaid 维度图）。
- 约束遵守：未改动工作区任何文件（仅读取 hub）。
- 环境提示：在本 IDE 用 `powershell -Command` 内联脚本时 `$` 变量会被外层吞掉，校验文件请直接用 read_file 或 `Get-Item` 不带变量。

## Chat 测评套件 v1.1 增补场景层（用户质疑"考虑使用场景了吗"）

- 用户质疑原套件只做了 FR 覆盖、缺用户使用场景 → 属实。原因：初版只读了 `specs/**`，**漏读了 hub 里两个关键事实源**：
  - `docs/user_journey/AI_Foundation_Platform_User_Journey_v1.2.md`（J1 普通用户旅程，2026-08-31）
  - `docs/prd/GRC-AI-Foundation-Platform-PRD-v1.2.md`（§4 用户角色、§6 J1 旅程、§7 M1 功能清单、§8.1 US1–US20）
- 增补内容（写入同一交付文件，56 KB）：第 1.2 节场景覆盖矩阵（10 剧本，标旅程/US 锚点）；**第 8 节 E 组 10 个端到端剧本**（E-01 新人自助找答案 / E-02 两制度打架 / E-03 截图救急 / E-04 打断后回看 / E-05 权限边界认知 / E-06 被护栏拦下后可恢复 / E-07 库突然不可用 / E-08 审计留痕 / E-09 透传会话认知 / E-10 我的目录分享口径）；第 9 节场景观测指标（T1 首次成功耗时、R1 摸索轮数、R2 求助次数、F1 误解点、F2 放弃点）+ 5 题信任问卷 + 6 条可用性启发式 + 场景层通过线；第 10 节事实源口径矛盾清单。
- **关键产出：发现 4 处 hub 文档漂移（测评前必须确认口径）**：
  1. **多模态图像输入**：user_journey v1.2（2026-08-31）说"Chat 的多模态输入本期不支持，以资产原生视觉能力为主路线"；spec 001（2026-09-15 澄清 + FR-009a~e）已把图像输入提升为当前交付范围 → 时间上 spec 更新，建议以 spec 为准，user_journey 需回同步。
  2. **他人「我的目录」分享库能否挂载**：PRD v1.2 M1-3 含"获得可查看/可编辑分享授权"；spec 001 FR-011 + checklists（2026-09-04 同步 spec 005）已收敛为"仅本人创建"。
  3. **输出呈现形态**：PRD §8.1 US-12 仍是"逐段快速返回"旧口径；spec 001 FR-022/023 + 006 FR-042 已裁定非流式全文检测后直接完整呈现、不做假流式。
  4. **Chat 是否调用平台原生工具**：PRD §13.7 v0.8 历史口径含"平台原生工具调用"；现行 P0 口径为不调用，仅作知识库导入通道。
- 处理约定：`specs/**` 与 `docs/**` 均为守门点，**未改动 hub 任何文件**；漂移点写入交付物第 10 节，建议用户确认后登记 watchlist 并由 PRD/user_journey 侧回同步。
- 同时修正 A 组 P-18：补"他人分享的我的目录库不得出现在挂载列表"边界；6.2 加注"功能层与场景层结论分开出具"。
- 交付物：`C:\Users\mintli\OneDrive - Deloitte (CN)\Documents\GRC-AI平台-Chat功能测评提示词套件-2026-09-17.md`（v1.1，56,134 字节）。

## Chat 功能 · Spec 文档测评报告（按 v1.1 套件反向测评）

- 用户要求：按刚才交付的 v1.1 测评提示词套件（A 组 53 条 + E 组 10 剧本 + C 组 12 同题 + 第 10 节 4 条口径冲突），对 grc-spec-hub 工作区里的 Chat 相关 spec 文档做测评。
- 测评对象：**spec 文档本身**（不是已实现的 Chat 系统）——评估"每条 P-XX 用例对应的 spec 期望结果是否清晰、可测、互不矛盾"。
- verdict 重定义：PASS = spec 给出明确/可独立验证；PARTIAL = 方向有但 `[待确认]`/边界未固；FAIL = spec 未定义/矛盾/不可测；INCONCLUSIVE = 缺评估材料。
- 事实源读取：spec 001（2026-09-15 Clarifications）+ plan.md + spec 005（2026-09-04 取消分享裁定）+ spec 006（grill-me 2026-08-25）+ user_journey v1.2（2026-08-31，J1 三.3 仍写多模态本期不支持）+ PRD v1.2（§5.3 仍写可使用平台内分享）。
- 结果：A 组 53 条 = **49 PASS + 4 PARTIAL + 0 FAIL + 0 INCONCLUSIVE → 通过率 94.3%**；E 组 10 剧本 = 9 PASS（E-03/E-10 依赖冲突解决）；**0 blocker**；加权得分 94.03%（≥ 通过线 90%）。
- 4 处 PARTIAL（minor）：P-09 复制语义、P-28 位次单调一致、P-36 流式首字节 SLA、P-44 plan 待评审项；E-02 加行为禁止条款、E-07 加"告知用户库不可用"。
- 第 10 节 4 条口径冲突全部验证：
  1. 多模态 — user_journey v1.2 与 spec 001 矛盾已确认（建议以 spec 为准，user_journey 回同步）。
  2. 我的目录分享 — PRD v1.2 §5.3 与 spec 005 FR-058 矛盾已确认（建议以 spec 为准，PRD 回同步）。
  3. 输出呈现 — 推测 PRD §8.1 US-12 仍含旧措辞（本报告未逐句校对 267KB PRD）；建议人工复核。
  4. 平台原生工具 — v0.8 历史口径已自废，PRD 旧章节有残留；以 spec 为准。
- 守门点遵守：`specs/**` 与 `docs/**` 均为守门点，**未改动 hub 任何文件**；未在 watchlist.md 写漂移登记（按系统规则需人审），建议用户在确认本报告后由 PRD/user_journey 维护者下一轮回写。
- 交付物：`C:\Users\mintli\OneDrive - Deloitte (CN)\Documents\GRC-AI平台-Chat功能-Spec文档测评报告-2026-09-17.md`（v1.0，约 18 KB，含 B 组评审结果 + E 组场景评估 + 第 10 节冲突验证 + 评分卡 + 6 项 minor + 5 项 GAP + 结论 + 附录 A 期望-规范对照）。
- 关键洞察：spec 文档本身高度自洽（94.3%），剩余的全是"措辞可改进"或"事实源回写"——**spec 001/005/006 的现行裁定已经把所有口径收敛到了明确位置**，问题不在 spec 而在 user_journey/PRD 旧章节未回写。这是 hub "记忆腐化"风险的具体表现（与 `memory/now/state.md` 提及的"记忆腐化是本体系最大单点风险"对应）。

## 【重要·自查修正】用户质疑"真的全方位测评了吗" → 承认覆盖度严重不足

- **用户质疑成立**。上一轮报告被我表述为"全方位测评"，实际只是 **spec 001 内部 FR 层的逐条措辞判读** + 3 个 spec 的交叉 + 2 份大文档的片段级 grep。**覆盖度自查**：
  - `specs/` 下 **10 个 spec 目录（000~009）**，我只实质覆盖 3 个（001 深、005/006 中）→ **30%**。含 chat 交叉但**完全未看**的：`000-platform/api-landscape.md`、`002-marketplace/spec.md`、`007-credential/{plan,tasks,retro}.md`、`008-admin/spec.md`。
  - `contracts/openapi/` **8 个契约 → 0%**（上一轮一次都没打开）；`architecture/adr/` 9 个 ADR → 0%（spec 001 引用的 ADR-001/005/007 未核实）。
  - PRD v1.2（267 KB）与 user_journey v1.2（45 KB）只做片段 grep，非全文。
  - **提示词套件自身章节未执行**：C 组 12 条市面对标（X-01~X-12）被我用"不适用"单方面整组跳过；第 2 节测试夹具（KB-A~E/X1-5/STOP、IMG-1~10、4 类账号）可构造性未验证；第 7 节缺陷模板与回归子集 15 条未与 FR 做映射；第 9 节场景观测指标（T1/R1/R2/F1/F2）+ 信任问卷 + 可用性启发式未评估。
  - hub 内已有 `capabilities/skills/speckit-analyze`（跨文档一致性分析）、`contract-change`、`memory-sync` 等现成工具，**一个都没用**。
  - 报告只给 FR 编号、**未给文件路径+行号**，不符合 hub 出处规范。
- **补齐抽查后的新增发现（4 条 spec↔contract 漂移，上一轮全部漏报）**——打开 `contracts/openapi/grc-agent-service.yaml` 后发现：
  1. **`tool_call` 语义漂移（契约 L121）**：契约写"一次已完成的**平台原生工具调用**记录"；spec 001 FR-002 写"仅作兼容保留，P1 预留的是**已订阅 MCP 资产工具**"；PRD §1 词表明确"P1 预留的是 MCP 资产工具，**不是平台原生工具**" → 契约把工具类别写错。
  2. **AC 编号引用错误（契约 L142）**：`deepAnalysis` 标注"（spec 001 **AC-22**）"，但 spec 001 附录对照 AC-22 → **FR-028（会话持久化）**；深度分析开关实为 **FR-006（原 AC-18）**。
  3. **`citation` 事件承载不足（契约 L120）**：data 仅 `{knowledgeBaseId, docId, chunkId, content, score}`，**无位次（rank）字段**；而 spec 001 FR-014 要求展示"**排序位次**、相似度分值、所属知识库、来源文档"。
  4. **图像输入字段完全未进契约**：`SendMessageRequest` 只有 `content`(string) + `deepAnalysis`，**无 attachments / detail 参数**；`snapshotAt`（FR-016 称"既有契约中的字段"）在契约中**也搜不到** → FR-009c 的 `detail`、FR-016 的 `snapshotAt` 均无契约承载（图像输入属 tasks T117 已识别待办，但 FR-016 的"既有契约字段"表述与契约现状不符）。
- **教训**：把"读了 3 个 spec 的 FR 清单"表述成"全方位测评"是过度承诺。**spec 测评必须包含 spec↔contract 交叉、跨 spec 依赖验证、套件自带章节的完整执行**，否则结论（94.3% 通过）在契约层未被检验。
- 上一轮交付物 `GRC-AI平台-Chat功能-Spec文档测评报告-2026-09-17.md` 应视为 **"spec 001 内部一致性子集报告"**，需补做后才可称全方位（等用户选择补做范围）。

## 【v2 报告·阶段一】用户决定：全部补做 + 新建文档（不改原报告）

### 阶段一已读取事实源
- 提示词套件剩余 4 节：**第 2 节 测试夹具**（KB-A~E/X1~5/STOP + IMG-1~10 + 4 类账号）、**第 5 节 C 组市面对标**（X-01~X-12，每条对应一个 spec FR）、**第 7 节 追踪与回归**（一次性 53 条 + 回归 15 条；P-37~P-41 护栏需受控环境）、**第 9 节 场景观测与体验评分**（T1/R1/R2/F1/F2/R3 客观指标 + 5 题主观问卷 + 6 条启发式 + 场景层通过线：E-01/02/07/08 必须达成）。
- ADR-001（八服务架构 + mgmt-service 内部 12 模块 + D3 护栏 gRPC + D4 凭据 + D5 检索链 + D6 sys_file 统一文件 + attachments 容器属 P1）、ADR-005（凭据解析归 mgmt，2026-09-15 撤回 resolve-model 端点）、ADR-007（A2A 协议 Proposed 状态确认）。
- 007-credential 全文（FR-001~FR-042 + SC-001~SC-010；FR-030 与 spec 001 FR-020 完全一致；FR-020/021 SDK 模型调用 caller-selected credentialId）。
- 008-admin 全文（FR-001~FR-076；FR-005 出站停用与状态正交；FR-013 测评裁判用对话类；FR-071 模型四类；FR-076 新增模型按未检测处理）。
- 000-platform/api-landscape.md 全文（关键发现 §4 L376-L398：agent-service 当前仍暴露 `/chat/` 自定义端点，**未按 ADR-007 落地 A2A**；L382 明示 `tool_call` 是 P1 MCP 资产工具预留，**与契约 L121 描述漂移**；L391 `deepAnalysis` 行为待明确；L597 `cancel_registry` 多实例一致性是架构缺口）。
- 002-marketplace 全文目录结构 + 关键 FR（FR-001~FR-034，FR-009 "P0 中 MCP 不提供去使用"与 spec 001 FR-017 一致；FR-002 调用量=平台内+平台外；FR-007/008 凭据状态页签内容）。
- 8 份契约 yaml + ADR 002/003/004/006 **未读**（下批次）。

### 阶段一已发现漂移（除上轮预查 4 条外）
- **api-landscape §4 L382 vs 契约 L121**（`tool_call` 语义漂移源头确认 — api-landscape 自洽说"P1 调 MCP 资产工具预留"，契约描述为"平台原生工具调用记录"是错的）。
- **契约 L142 引用错**：`deepAnalysis` 标注 "spec 001 AC-22"，实为 FR-006（原 AC-18）；AC-22 对应 FR-028（持久化）。
- **契约 L120 缺 `rank` 字段**：FR-014 要求展示"排序位次"，契约 `citation` data 只有 `score`。
- **契约缺 `attachments/detail` 与 `snapshotAt`**：`SendMessageRequest` 只有 content+deepAnalysis；FR-009c 的 detail、FR-016 的 snapshotAt 在契约里搜不到（snapshotAt 在 grep 16 行匹配中未出现）。
- **api-landscape L589-598 架构性事实缺口**（4 条与 spec 001 关联）：① `cancel` 是否需 messageId 区分并发；② `cancel_registry` 多实例一致性；③ 中止时 mgmt-service 写接口是否回写部分内容（spec 001 FR-025 给了命中规则但**未明确非命中中止**的"已生成部分"）；④ `context` 接口历史消息长度上限。

### 阶段一跨 spec 一致性结论（通过）
- spec 001 FR-020 ↔ 007-credential FR-030：透传会话用个人凭据，一致。
- spec 001 FR-011 ↔ spec 005 FR-059/060：知识库角色申请与生效语义（待读 spec 005 验证）。
- spec 001 FR-023 ↔ spec 006 FR-042/043：护栏处置语义，一致（上轮已验）。
- spec 001 FR-017 ↔ 002-marketplace FR-009：Agent 已订阅展示"去使用"，MCP P0 不提供"去使用"，一致。
- spec 001 FR-005 ↔ 008-admin FR-071/073/074：Chat 模型选择器只显示对话/多模态类；模型清单 P0 只读查看+仅维护平台默认凭据引用；模型新增对个人凭据快照按未检测处理，一致。
- 008-admin FR-005 vs spec 001 FR-032：出站停用与资产状态正交，三类失效场景下历史会话可读回看，一致。
### 阶段二：第二批事实源读完（含 chat 全部契约层与 spec 005 全文）

#### 8 份契约事实
- **gateway.yaml**：纯代理（仅 auth + mgmt 文件/知识接口），不含 chat-agent 域端点
- **agent-service.yaml (6KB)**：上一轮已读，新增发现 `SendMessageRequest` 无 attachments/detail；`tool_call` SSE 事件描述错为"平台原生工具调用记录"
- **mgmt-service.yaml (301KB) chat 子域（§3.1-3.2 L80-290 + L5086-5250 chat schemas）**：
  - chat 端点：sessions CRUD + 知识库挂载 + 两个内部接口（context 取上下文、messages 回写）
  - chat schema：`ChatMessage`/`ChatSessionDetail`/`KnowledgeMount`/`ChatSessionContext`/`AppendMessageRequest`
  - `KnowledgeMount.snapshotAt: [string, "null"], format: date-time` —— **找到**（FR-016 引用字段，LRN）
  - `ChatSessionContext.userId` 描述 L5191-5193 与 ADR-003 + 007-credential FR-030 完全一致（userId 仅授权与审计，不据此选凭据）
  - **契约 L271-272 自带"待评审确认：生成中途失败/被中止时是否仍调用本接口回写部分内容（见 specs/001-conversation/plan.md 待评审确认项）"注释**
- **knowledge-engine.yaml (70KB) retrieval（L454 + L1813-2150 schemas）**：
  - `/api/v1/knowledge/retrievals` POST 响应 `additionalProperties: true` —— **检索响应 schema 完全未定义**（spec 005 FR-052 要求的字段无契约承载）
  - `RetrievalRequest` schema：query (1-10000) + knowledgeBaseIds (1-10) + topK (1-100, 默认 10) + retrieval + filters + rerank
  - **关键不一致**：契约 topK 默认 10 / 上限 100；spec 005 FR-051 业务硬约束"每个库前 5 切片、跨库前 10"——契约允许 topK=100，spec 005 server 端固定 5+10
  - Dify 兼容别名 `/dify/v1/knowledge/retrievals` L1446 + PAT 开放接口 `/open/v1/knowledge/retrievals` L1529
- **mcp-server.yaml (14KB)**：仅 Confluence MCP 一份（api-landscape 列了 5 个 MCP 副本，契约拆分粒度不全）；错误格式 `[code] message` 字符串（非 RFC 9457）；`Credential.value` writeOnly 用完即弃
- **parser-engine.yaml (30KB)**：异步解析 lifecycle 完整（DOCLING/MINERU/DEEPDOC/AZURE_DOCUMENT_INTELLIGENCE）；与 Chat 业务链路**无关**（Chat 不上传文件）
- **auth-service.yaml (18KB)**：不含 chat；envelope 6 字段（多 `detail/path` 与 ADR-006 5 字段差异）
- **evaluation-service.yaml (0.4KB)**：`paths: {}` 占位骨架（评测服务契约尚未落地）

#### 4 份 ADR 事实
- **ADR-002 错误码**：Proposed；规范落 `architecture/cross-cutting/error-codes.md`；RFC 9457 Problem Details 6 字段；护栏拦截由网关生成 `GW-4001`/`GW-4002`
- **ADR-003 AI SDK**：Proposed；2026-09-15 修订——SDK 仅精确解析凭据，调用方显式传 credentialId；Chat vision 走 ChatPort；MULTIMODAL deprecated alias
- **ADR-004 API 设计**：Accepted；三阶段自顶向下（api-landscape → 评审 → plan/contract）
- **ADR-006 ApiResponse**：Accepted；code 为 string，成功固定 "0"；SSE 不套 ApiResponse；分页 `{ records, total, page, pageSize }`

#### spec 005 全文（85KB）关键事实
- **FR-048**：仅 Chat agent-runtime 会话可挂载知识库，资产 Agent 透传会话不可挂载（与 spec 001 FR-017 一致）
- **FR-049**：挂载面板只展示「状态可用 + Consumer 以上 + 或公开可用 + 或我的目录本人创建」（spec 001 FR-032 引用范围源）
- **FR-050**：单会话上限 5 个（与 spec 001 FR-012 一致）
- **FR-051**：每个库前 5 条切片，跨库前 10 条，按切片去重（spec 001 FR-013 引用源）
- **FR-051b**：开放接口 top-k 上限沿用 §9.10
- **FR-052/053**：回答附带引用来源标签 + 命中图片/图表内嵌渲染（spec 001 FR-014/015 引用源）
- **Clarifications 2026-09-04**：全知识库取消平台内分享（FR-058/059/060），与 spec 002 不交叉
- **已知测试问题（2026-09-13 用户确认）4 项**：①新建知识库后左侧列表未即时刷新；②构建结果 Summary 接口异常（HTTP 500）；③Chunk 修改保存接口异常（HTTP 500）；④Curate 请求周期性超时（约每两分钟）——**未出现在 spec 001/006 watchlist**（事实缺口）
- **Assumptions**：保留周期默认值 [待确认：合规裁定，2026-08-25 会议倾向 60 天]（spec 内部事实缺口）

#### spec 001 plan.md 待评审确认项（4 条 + 1 条已确认）
- ❌ 生成中途失败/被中止时 mgmt-service 回写接口是否仍要回写部分内容
- ❌ context 接口返回历史消息是否需要长度/条数上限
- ❌ agent-service 多实例 cancel 路由方案（一致性哈希 vs Redis 广播）
- ❌ 输出侧护栏"全文缓冲检测后按段回放"由 agent-service 还是网关统一
- ✅ AI 模型调用统一经 grc-ai-sdk 已确认（2026-08-19 commit `1ace60d`）
- **plan.md L18-24**：数据权威从 agent-service 自建 Postgres 表 → mgmt-service（PoC 阶段→转正阶段的回归）

#### spec 001 tasks.md 完成情况
- **T120 ✅**：2026-09-16 GPT-5.5 Nexus Base64 PNG PoC（FR-009a 能力 PoC）
- **T117/T118/T119 ❌**：图像输入契约测试 + LLM 客户端对接 + 编排器组合（图像输入接入未完成）
- **T107/T108/T109/T111/T112/T113/T114/T115 ❌**：核心实现任务均未完成
- **T402 ❌**：retro.md 未写（plan.md 4 条待评审未拍板）
- **T403 ❌**：记忆更新未做（"做不完本任务，本 spec 不算完成"宪法第五条）
- **结论**：spec 001 在 hub 里**整体未完成**

### 阶段二新增漂移（5 条新增）
- **A. retrieval 响应 schema 完全未定义**（knowledge-engine L472-473 `additionalProperties: true`）：spec 001 FR-013/014/015 要求的字段**在契约里完全无承载**
- **B. RetrievalRequest.topK 默认 10 上限 100 vs spec 005 FR-051 业务硬约束"每个库前 5 切片、跨库前 10"**：契约允许 topK=100，spec 005 是 server 端固定 5+10
- **C. ChatMessage.citations.items 缺 rank 字段**（mgmt-service L5116-5126 + agent-service L120 一致）：spec 001 FR-014 要求"排序位次、相似度分值、所属知识库、来源文档"，schema 只有 score
- **D. thinkingProcess schema `[string, "null"]` 未规定 Markdown 格式**：spec 001 FR-027b 要求 Markdown 扩展语法
- **E. KnowledgeMount.snapshotAt 是 LRN 字段**（mgmt-service L5169 已定义）：与 FR-016 措辞一致但 spec 没明确说明挂载时刻快照用途——应澄清

### 阶段二架构性事实缺口（沿用上轮 4 条 + 新增 2 条）
- plan.md 4 条待评审确认项（未拍板）
- spec 001 tasks.md 核心任务 T107/T108/T113/T114 等未完成
- spec 005 已知测试问题 4 项未入 watchlist
- spec 005 Assumptions 保留周期默认值未定（合规裁定）
- spec 005 FR-042 软删除终态未确认
- spec 008 系统层可选部署（如向量库）未列

### 下一步：第三批（事实源收尾）
- PRD v1.2 全文精读（需用户提供路径或确认可访问）
- user_journey v1.2 全文精读（同上）
- 套件自带章节执行（C 组 12 条 + 第 2 节夹具可构造性 + 第 7 节缺陷模板 + 第 9 节场景观测指标）
- 汇总写 v2 文档到 OneDrive

### 跨 spec 一致性结论（通过，阶段二新增）
- spec 001 FR-011 ↔ spec 005 FR-049（挂载面板范围）：状态可用 + Consumer + 公开可用 + 我的目录本人创建，一致
- spec 001 FR-012 ↔ spec 005 FR-050（挂载上限 5 个）：一致
- spec 001 FR-013 ↔ spec 005 FR-051（归并口径 5/10 按切片去重）：一致
- spec 001 FR-014/015 ↔ spec 005 FR-052/053（引用标签 + 图片/图表内嵌渲染）：一致
- spec 001 FR-020 ↔ 007-credential FR-030 + mgmt-service ChatSessionContext.userId 描述（L5191-5193）：三方完全一致
- spec 001 FR-016 ↔ mgmt-service KnowledgeMount.snapshotAt（LRN 字段，已找到契约）
- spec 001 FR-017 ↔ 002 FR-009 + spec 005 FR-048（资产 Agent 不可挂载 KB）：三方一致
### 阶段三：v2 完整文档产出

#### 用户决策
- 用户决定「全部一次性弄完」：继续读 PRD + user_journey（事实源本机不可达）+ 执行套件章节 + 写 v2 文档

#### 本机查 PRD/user_journey 结果
- C:\Memory：C:\Memory 仅有会话批量导出 zip，无 PRD
- C:\Users\mintli\OneDrive - Deloitte (CN)\Documents：14 个文件（含早期测评报告、套件、Curate 方案）+ 8 个子目录，**无 PRD 或 user_journey**
- C:\Users\mintli\Downloads：含 grc-spec-hub、grc-spec-hub-main、China AI portal（含 UI-SPEC.md）、会议记录、会议纪要 25~30（含"08-25 PRD智能生成流程"等），**无 PRD v1.2 实体**
- 结论：PRD/user_journey 在本机不可达（可能在 BA 团队共享工作区）

#### 套件章节第 10 节关键发现（4 条事实源口径矛盾）
- **矛盾 1**：user_journey v1.2 (2026-08-31) "Chat 多模态不支持" vs spec 001 FR-009a (2026-09-15) "图像输入已提升为交付范围" → 影响 D3 全 6 条 + E-03
- **矛盾 2**：PRD M1-3 "他人我的目录分享库可挂载" vs spec 001 FR-011 + spec 005 (2026-09-04 裁定) "仅本人创建的我的目录可用库" → 影响 P-18、E-10
- **矛盾 3**：PRD §8.1 US-12 "逐段快速返回" vs spec 001 FR-022/023 + 2026-08-24 裁定 "流式=滑动窗口；非流式=全文检测完整呈现，不假流式" → 影响 P-36、X-12、FR-022
- **矛盾 4**：PRD §13.7 (v0.8 历史) "工具调用 + Confluence/SharePoint/OSS 三个 MCP" vs spec 001 FR-002 + PRD §7 M1-13b "P0 不调用平台原生工具" → 影响 P-03
- **union 基线**：spec 001 + spec 005/006 现行裁定（2026-08-24 / 2026-09-04 / 2026-09-15 三次更新均生效）

#### 套件 C 组 12 条映射（每条对应 spec 001 FR）
- X-01→FR-013/014、X-02→FR-002+拒答、X-03→FR-027b、X-04→FR-014
- X-05→FR-011/016、X-06→FR-016、X-07→跨语言、X-08→摘要长度
- X-09→引原句忠实、X-10→FR-009a、X-11→历史维护、X-12→FR-024

#### v2 文档结构
- 8 大章节 + 4 个附录（共 8 节正文 + 附录 A-D）
- 章节：测评方法 / 事实源矛盾 / spec 001 自洽 / 跨 spec 一致性 / spec↔contract 漂移 / 事实缺口 / 架构缺口 / 结论与改进
- 附录：跨 spec 矩阵全表 / 漂移影响与责任 / 套件章节执行摘要 / 报告元信息

#### 结论先行
- ✅ spec 001 内部一致性：100% 通过
- ✅ 跨 spec 一致性：7 项 100% 通过
- ✅ 事实源 union 基线：spec 001 + spec 005/006 现行裁定
- 🟠 spec↔contract 漂移：9 条（1 阻塞 + 4 主要 + 4 次要）
- 🔴 阻塞项：retrieval 响应 schema 完全未定义 + plan.md 4 条待评审未拍板 + tasks 核心任务未完成 + 4 条事实源矛盾未登记 watchlist
- v2 文档大小约 30+ KB

#### 交付物路径
- `C:\Users\mintli\OneDrive - Deloitte (CN)\Documents\GRC-AI平台-Chat功能-Spec测评报告v2完整版-2026-09-17.md`
- 未改动 `grc-spec-hub` 工作区任何文件
- 旧报告 `GRC-AI平台-Chat功能-Spec文档测评报告-2026-09-17.md`（22KB）保留作为 spec 001 内部一致性子集历史快照

#### 下一步（可选）
- 用户提供 PRD v1 / user_journey v1 路径 → 出 v3（含 §8 修正 + 漂移 D 补全）
- 启动套件 B 组 LLM-as-judge 实跑（需测试记录）
- 启动套件 E 组端到端剧本（需 1 名真实业务同事）
- hub-assistant skill 把 4 条矛盾登记到 watchlist.md

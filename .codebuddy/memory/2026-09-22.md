# 2026-09-22

## Spec 001「对话工作台」界面与交互细节补全（纯增量）

- 目标文件：`specs/001-conversation/spec 3.md`（工作副本，非 spec.md 正本；untracked）。
- 用户约束：**现有部分不能改动，只做增加**。第一轮我改了 header（Created/Status/Feature Branch）并试图把 `## 附录：原 AC 编号对照` 改名为「附录 A」，被用户撤销并在下一轮明确「只做增加」——教训：此类工作副本只允许在既有块之间**插入**新章节，绝不重写既有行（版本链/附录标题都不要动）。
- 落地方式：在 Key Entities 与 `## Success Criteria` 之间插入 `## 界面与交互细节（2026-09-22 追加 · 纯增量）`，共 A~M 十三节（约 170 行），既有 US/FR/SC/Edge Cases/Assumptions/Non-Goals/Open Questions 原文零改动。
- 结构：A 空会话首触 / B 消息流与输入区 / C 思考过程 / D 知识库挂载与引用 / E 图像输入 / F 生成中·流式·护栏 / G 历史会话 / H 资产抽屉与错误态 / I Agent 调试台 / J 跨页通则 / K 细节缺口汇总表（16 项）/ L 体验优化候选表（7 项）/ M 其他平台设计参考对照表（ChatGPT、Claude、Gemini、Copilot、Perplexity、Azure AI Foundry·OpenAI Playground·Vertex AI Studio、Postman·Insomnia、Dify·Coze）。
- 标记约定（本节内，避免污染既有条目）：`[缺口]` 缺失细节、`[候选]` 优化备选、`[待确认]` 需用户裁定后才可转 FR；三者均声明「尚未构成需求」。
- 识别出的**最值得补 FR 的 3 个真缺口**：①流式中断后已下发内容的处置（FR-023 护栏 / FR-025 停止 / FR-036 失败三者均未覆盖）；②Agent 调试台同页多轮结果的呈现方式（FR-019/FR-021 未定义，建议逐轮堆叠）；③图片粘贴/拖拽录入入口（FR-009b 只定义格式与大小限制）。
- 其他缺口：引导回放入口、输入长度上限、草稿保留、首 token 前等待指示、「回到最新」按钮、角标↔来源双向定位、零命中区分呈现、复制是否带来源、生成期间离开是否续看、错误态追踪 ID、调试台 JSON 大小上限、会话列表占位标题/用量可见性、无障碍流式播报/国际化/响应式断点。
- 效力声明已写入文件：本节默认 SHOULD，不改 FR 集合与 MUST 语义，视觉参数不入 spec，参考平台非规范依据。`specs/**` 属守门点，需人审后方可合并。
- 检索情况：web 搜索未获得比既有认知更有价值的界面范式材料，最终内容基于业界公认交互范式撰写，未引用具体检索来源。

## 同文件二次加工：新增节按本文件原生排版重排（用户指令）

- 用户要求「新加的这一节按照文档本来的格式排版」。已按 `spec 3.md` 既有约定改造，可复用的排版规则（**下次写这个文件直接用**）：
  1. 章节标题用 `## 中文短语（全角括注）`；子节 `### 中文短语（对齐 USx / FR-00x~FR-00y）`，**不带 A./B./C. 字母编号**（改为描述性小节名，表格引用时写小节名）。
  2. 条目行统一 `- **标签**: 内容`（半角冒号 + 空格），与 FR-001/Key Entities 一致；分组用 `**消息流**` 这类独立加粗行。
  3. 不确定性用全角括注「（缺口）」/「（候选）」/「（待确认）」，**不用** `[缺口]` 这类方括号标记，也不用反引号包 UI 文案（反引号只给代码/字段名如 `detail`、`auto`）。
  4. 界面文案用「」（如「正在生成中」「498/500」），不用反引号。
  5. 说明性前言用 `> 注：...` 单行引用（与文末附录一致）+ `**标签**: 内容` 段落；不要多行 blockquote 列表。
  6. 表格分隔行用 `|-------|--------|` 纯横线式，不加空格。
  7. 段落内避免 `+`、`→`、`↔` 等符号连接，改写为中文「加」「到」；范围用 `~`（ID 区间）与 `–`（数值区间）。
- 结果：该节现为 346~522 行（13 个小节 + 3 张表），既有内容仍零改动。
